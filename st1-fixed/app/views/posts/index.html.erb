<h1>What's up!</h1><br/>
<h2>This is the StanceTrader Car Parts page</h2>
<br>
	
<%= link_to 'New post', new_post_path %>
<br>
<br>
<table>
	<tr>
		<th>Name of Car Parts</th>
		<th>Car Parts Description</th>
		<th></th>
		<th></th>
	</tr>

	<% @posts.each do |post| %>
		<tr>
			<td><%= post.title %></td>
			<td><%= post.text %></td>
			<td><%= link_to 'Show', post_path(post)%></td> <!-- (post) is an argument for post_path -->
			<td><%= link_to 'Edit', edit_post_path(post) %></td>
			<td><%= link_to 'Destroy', post_path(post), method: :delete, data: { confirm: 'You sure you wanna delete this?' } %></td>
		</tr>
	<% end %>
</table>
<br>
<b><%= link_to 'Homepage', root_path %></b>